#include <bits/stdc++.h>
using namespace std;

bool ok(int m) {
  // TODO: to be implemented
}

/**
 * The function must be a monotonic function matching the binary search type
 * (min or max).
 */

int binarySearch() {
  int ret;
  int l = 0, r = 1e9;
  while (l <= r) {
    int mid = l + (r - l) / 2;

    if (ok(mid)) {
    } else {
    }
  }
}
